{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ensim.dev/schema/interaction.schema.json",
  "title": "Interaction",
  "description": "A structural (doctrinal), not mission-instance-specific, edge in the mission execution interaction network: who or what interacts with who or what, and how. Sits alongside DoctrineProcess as a description of how the enterprise normally operates, at a finer grain — individual personnel and the systems they use, rather than organizational tasking flow. See ARCHITECTURE.md Section 6.",
  "type": "object",
  "properties": {
    "id": { "$ref": "common.schema.json#/$defs/entityId" },
    "from": { "$ref": "common.schema.json#/$defs/participant" },
    "to": { "$ref": "common.schema.json#/$defs/participant" },
    "interactionType": {
      "type": "string",
      "enum": ["Operates", "CommunicatesVia", "AuthenticatesTo", "CoordinatesWith", "Commands"]
    },
    "description": { "type": "string" },
    "doctrineSource": {
      "type": "array",
      "items": { "$ref": "common.schema.json#/$defs/doctrineSource" },
      "minItems": 1
    }
  },
  "required": ["id", "from", "to", "interactionType", "description", "doctrineSource"],
  "additionalProperties": false
}
